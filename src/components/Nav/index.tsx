import React from "react";

import { Container, InnerContainer, Logo } from "./styles";

const Nav = () => {
  return (
    <Container>
      <InnerContainer>
        <Logo href="/">@sksuryan</Logo>
      </InnerContainer>
    </Container>
  );
};

export default Nav;
